/** @type {import('tailwindcss').Config} */
import {
  Input,
  Ripple,
  initTE,
} from "tw-elements";

initTE({ Input, Ripple });
module.exports = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
}

